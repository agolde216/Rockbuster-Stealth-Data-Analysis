SELECT MIN(release_year) AS min_release_year,
	MAX(release_year) AS max_release_year,
	MODE() WITHIN GROUP (ORDER BY language_id) AS mode_language_id,
	MIN(rental_duration) AS min_rental_duration,
	MAX(rental_duration) AS max_rental_duration,
	CAST(AVG(rental_duration) AS DECIMAL (3,2)) AS avg_rental_duration,
	MIN(rental_rate) AS min_rent,
	MAX(rental_rate) AS max_rent,
	CAST(AVG(rental_rate) AS DECIMAL(3,2)) AS avg_rent,
	MIN(length) AS min_length,
	MAX(length) AS max_length,
	CAST(AVG(length) AS DECIMAL (3)) AS avg_length,
	MIN(replacement_cost) AS min_replacement,
	MAX(replacement_cost) AS max_replacement,
	CAST(AVG(replacement_cost) AS DECIMAL (3)) AS avg_replacement,
	MODE() WITHIN GROUP (ORDER BY rating) AS mode_rating
FROM film;
